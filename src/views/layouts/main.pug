doctype html

html(lang="pt-BR")

	head

		meta(charset="UTF-8")
		meta(name="viewport" content="width=device-width, initial-scale=1")

		block title

		link(rel="stylesheet" href="/styles/GlobalConfigs.css")
		block styles

	body

		block content
